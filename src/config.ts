import { ContractConfig } from "./types.ts";

export const budConfig: ContractConfig = {
  extra: {
    outRef: {
      txHash: "",
      outputIndex: 0,
    },
    initialOwners: ["", "", "", ""],
  },
  oldPolicyId: "",
  deployTxHash: "",
};
